import { StyleSheet } from 'react-native';

export const appStyles = StyleSheet.create({
  button: {
    alignItems: 'center',
    borderRadius: 10,
    paddingHorizontal: 5,
    paddingVertical: 10,
    width: 80
  }
});
